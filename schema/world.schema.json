{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "World",
  "type": "object",
  "required": [
    "block_size",
    "connections",
    "nodes",
    "sample_rate"
  ],
  "properties": {
    "$schema": {
      "description": "Path to the JSON Schema for this file (used by editors).",
      "type": [
        "string",
        "null"
      ]
    },
    "block_size": {
      "type": "integer",
      "format": "uint",
      "minimum": 0.0
    },
    "connections": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Connection"
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/NodeDef"
      }
    },
    "sample_rate": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "definitions": {
    "Connection": {
      "type": "object",
      "required": [
        "from",
        "to"
      ],
      "properties": {
        "from": {
          "description": "Source port path: `<node_id>.<port_name>`",
          "type": "string"
        },
        "to": {
          "description": "Destination port path: `<node_id>.<port_name>`",
          "type": "string"
        }
      }
    },
    "NodeDef": {
      "type": "object",
      "required": [
        "id",
        "type"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "params": {
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "format": "double"
          }
        },
        "type": {
          "type": "string"
        }
      }
    }
  }
}
