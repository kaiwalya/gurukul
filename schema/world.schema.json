    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.06s
     Running `target/debug/gurukul emit-schema`
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "World",
  "type": "object",
  "required": [
    "connections",
    "nodes"
  ],
  "properties": {
    "$schema": {
      "description": "Path to the JSON Schema for this file (used by editors).",
      "type": [
        "string",
        "null"
      ]
    },
    "connections": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Connection"
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/NodeDef"
      }
    }
  },
  "definitions": {
    "Connection": {
      "type": "object",
      "required": [
        "from",
        "to"
      ],
      "properties": {
        "from": {
          "description": "Source port path: `<node_id>.<port_name>`",
          "type": "string"
        },
        "to": {
          "description": "Destination port path: `<node_id>.<port_name>`",
          "type": "string"
        }
      }
    },
    "NodeDef": {
      "type": "object",
      "required": [
        "id",
        "type"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "params": {
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "format": "double"
          }
        },
        "type": {
          "type": "string"
        }
      }
    }
  }
}
