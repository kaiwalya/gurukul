/target
**/*.rs.bk
.DS_Store
.moon/cache
.claude/

# Real-voice test fixtures live outside the repo for license/size
# reasons. Drop wav files into `*/test_data/` and run the ignored
# tests with `cargo test --release -- --ignored`.
**/test_data/

# UX flight-recorder output (apps/coach-game). One directory per run,
# gitignored and grown unbounded — manual cleanup. See
# docs/COACH_GAME_UX_TRACE_PLAN.md.
traces/
.scratch/

# Provisioning profiles — generated on developer.apple.com, kept out of git.
apps/coach-game/ios/*.mobileprovision

# Local environment config (personal — e.g. iOS signing). Template: .env.example.
/.env
